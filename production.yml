version: '3'

services:
    nginx:
        build: ./deploy/nginx/
        image: eoglethorpe/natoolbox:nginx-1.0
        ports:
            - '80:80'
        depends_on:
            - web
        volumes:
            - app-volume:/code
            - static-volume:/static
    web:
        build: .
        image: eoglethorpe/natoolbox:django-1.0
        command: ['./run_production.sh']
        volumes:
            - app-volume:/code
            - static-volume:/static
        env_file:
            - production.env

volumes:
    app-volume:
    static-volume:
